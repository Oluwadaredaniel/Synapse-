# API URL (Backend Address)
VITE_API_URL=http://localhost:5000/api

# --- FOR BACKEND REFERENCE (If you are running the backend in the same env) ---
# Ensure this matches the port in vite.config.ts
CLIENT_URL=http://localhost:3000